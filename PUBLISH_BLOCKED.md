# Publish blocked — pre-flight gate failed

`publish_repo.sh` refused to push `agentfuse-sdk` to GitHub. Fix the issues below, then re-run.

- Working tree is dirty. Commit or stash before publishing.
