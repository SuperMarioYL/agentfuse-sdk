[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentfuse"
version = "0.9.0"
description = "Reserve estimated per-task spend before wrapped LiteLLM calls, settle usage afterward, and block requests that exceed configured ceilings."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "supermario_leo" }]
keywords = ["agent", "llm", "budget", "circuit-breaker", "litellm", "cost-control", "spend-guardrail"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Monitoring",
]
dependencies = [
    "litellm>=1.40",
    "tiktoken>=0.7",
    "click>=8.1",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
agentfuse = "agentfuse.cli:main"

[project.urls]
Homepage = "https://github.com/SuperMarioYL/agentfuse-sdk"
Repository = "https://github.com/SuperMarioYL/agentfuse-sdk"

[tool.hatch.build.targets.wheel]
packages = ["src/agentfuse"]
