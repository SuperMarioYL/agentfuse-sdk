{
  "schema": 1,
  "name": "agentfuse-sdk",
  "github": "https://github.com/SuperMarioYL/agentfuse-sdk",
  "host": "agentfuse-sdk.lei6393.com",
  "lang": { "primary": "zh", "toggle": true },
  "accent": ["#dc2626"],
  "content_version": "v0.5.0",
  "hero": {
    "eyebrow": { "zh": "本地 · 无账号 · 两行接入", "en": "Local · No account · Two-line setup" },
    "headline": {
      "zh": "Agent 跑穷你之前\n先跳闸",
      "en": "Trips before your Agent\nburns your budget"
    },
    "sub": {
      "zh": "给单次任务设一个硬上限——下一次调用要把花费推过天花板时，在它发出之前就把循环掐断，钱根本没花出去。同步、异步、并发扇出、流式调用，天花板都成立。这是执行式熔断，不是事后画一张成本图。",
      "en": "Set a hard ceiling on one task. When the next call would push spend past it, the loop is cut before the call ever goes out — the money is never spent. Sync, async, parallel fan-out, streaming — the ceiling holds in every shape your Agent runs. An enforcing fuse, not an after-the-fact cost chart."
    },
    "command": "agentfuse demo",
    "cta": { "zh": "开始使用", "en": "Get started" },
    "ctaGithub": { "zh": "在 GitHub 查看", "en": "View on GitHub" }
  },
  "advantages": [
    {
      "title": { "zh": "钱花出去之前就拦下", "en": "Blocks the call before the money is spent" },
      "body": {
        "zh": "保险丝在调用发出之前估出这次的花费上界，一旦会把累计推过天花板就直接掐断——那次超预算的调用根本没发出去，钱也没花出去。就算 Agent 并发扇出多个调用，每次都先扣占位再发，谁也不会趁对方在途时溜过天花板。不是事后报账。",
        "en": "Before a call goes out, the fuse estimates its upper-bound cost; the moment that would push the cumulative total past the ceiling, it cuts in — the over-budget call is never sent and the money is never spent. Even when your Agent fans out parallel calls, each reserves its slice up front so none slips through while another is in flight. Not an after-the-fact report."
      },
      "chips": [
        "› 调用从不发出",
        "› 并发也拦得住"
      ]
    },
    {
      "title": { "zh": "两行接入，现有代码不用改", "en": "Two lines, your existing code untouched" },
      "body": {
        "zh": "把 Agent 的主调用包进一个作用域，或装一次接管 LLM 入口——你已经在用的调用方式会被自动拦截计量，不用为每个 provider 改一遍代码。同步函数和异步函数都一样：装饰器认得 async，预算作用域会一直撑到协程真正跑完，不会再被静默绕过。",
        "en": "Wrap your Agent's main call in one scope, or install once to take over the LLM entry point — the calls you already make get gated and metered automatically, no per-provider code rewrite. Sync and async functions alike: the decorator recognizes async, and the budget scope stays bound until the coroutine actually finishes — never silently bypassed again."
      },
      "chips": [
        "› 作用域即预算窗口",
        "› async 也照常拦"
      ]
    },
    {
      "title": { "zh": "流式调用也照算进账", "en": "Streaming calls count too" },
      "body": {
        "zh": "Agent 最常用的流式调用，过去会因为读不到用量而记成零花费、保险丝永远不跳。现在流一耗尽就按真实用量记账，拿不到用量就按调用前的保守上界记——而且这个上界会按模型实际能吐的长度收紧，不再是写死的小数字。累计熔断对流式照样生效。",
        "en": "The streaming calls agents actually use used to read as zero spend because usage arrived too late, so the fuse never tripped. Now each stream is metered on exhaustion — real usage when the provider emits it, else a conservative pre-call upper bound that tightens to what the model can actually output, not a fixed small number. The cumulative fuse holds on streamed runs too."
      },
      "chips": [
        "› 流耗尽即记账",
        "› 上界按模型收紧"
      ]
    },
    {
      "title": { "zh": "跳闸时通知你", "en": "Pings you when it trips" },
      "body": {
        "zh": "保险丝跳闸时，会带着这次的花费、上限和触顶原因回调你注册的钩子——接进你的告警、审计或计数，不用在每个任务外面再包一层 try/except。回调出错也不会影响是否拦下调用。",
        "en": "When the fuse trips, it calls the hook you registered with the spend, ceiling, and which limit blew — wire it into your own alerting, audit log, or counter, without wrapping every task in its own try/except. A callback that errors never changes whether the call is blocked."
      },
      "chips": [
        "› 跳闸即回调你",
        "› fail-soft 不误拦"
      ]
    }
  ],
  "scene": {
    "kind": "particle-field"
  },
  "cta": {
    "headline": {
      "zh": "Agent 跑穷操作者的事，别再重演",
      "en": "Don't let an Agent bankrupt its operator again"
    },
    "sub": {
      "zh": "一条 pip install，给你的下一个 Agent 装上保险丝。开源、Apache-2.0、永久免费。",
      "en": "One pip install puts a fuse on your next Agent. Open source, Apache-2.0, free forever."
    },
    "button": { "zh": "在 GitHub 上开始", "en": "Get started on GitHub" }
  },
  "footer": {
    "tag": {
      "zh": "给自主 Agent 装的执行式花费保险丝",
      "en": "The enforcing spend fuse for autonomous Agents"
    }
  }
}
